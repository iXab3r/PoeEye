<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:PoeEye.PoeTrade.ViewModels">
    <DataTemplate DataType="{x:Type viewModels:PoeItemTypeSelectorViewModel}">
        <Grid>
            <ComboBox Style="{StaticResource QueryComboBox}"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      IsTextSearchCaseSensitive="False"
                      StaysOpenOnEdit="True"
                      Text="{Binding SelectedValue}"
                      ItemsSource="{Binding KnownItemTypes}" />
        </Grid>
    </DataTemplate>
</ResourceDictionary>