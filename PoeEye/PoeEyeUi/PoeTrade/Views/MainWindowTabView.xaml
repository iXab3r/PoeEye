<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:poeEyeUi="clr-namespace:PoeEyeUi"
                    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls">
    <DataTemplate DataType="{x:Type poeEyeUi:MainWindowTabViewModel}">
        <Grid Margin="0, 10, 0, 10">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0"
                    Command="{Binding SearchCommand}" Content="Search"></Button>

            <controls:MetroContentControl Grid.Row="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl 
                     ItemsSource="{Binding TradesListViewModel.TradesList}" />
                </ScrollViewer>
            </controls:MetroContentControl>
        </Grid>
    </DataTemplate>
</ResourceDictionary>