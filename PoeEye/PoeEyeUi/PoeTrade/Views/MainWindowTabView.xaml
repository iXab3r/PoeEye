<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:poeEyeUi="clr-namespace:PoeEyeUi"
                    xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                    xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
                    xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase">
    <DataTemplate DataType="{x:Type poeEyeUi:MainWindowTabViewModel}">
        <Grid Margin="0, 10, 0, 10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Expander Header="Query" IsExpanded="true">
                <StackPanel>
                    <controls:MetroContentControl Content="{Binding QueryViewModel}"/>
                    <DockPanel>
                        <Button HorizontalAlignment="Right" Width="150" Command="{Binding SearchCommand}" 
                            CommandParameter="{Binding QueryViewModel.PoeQueryBuilder}" 
                            Content="Search"/>
                    </DockPanel>
                </StackPanel>
            </Expander>

            <controls:MetroContentControl Grid.Row="1">

                <controls:MetroContentControl.Resources>
                    <CollectionViewSource x:Key="cvsActors" Source="{Binding TradesListViewModel.TradesList}" >
                        <CollectionViewSource.GroupDescriptions>
                            <PropertyGroupDescription  PropertyName="TradeState"/>
                        </CollectionViewSource.GroupDescriptions>
                    </CollectionViewSource>
                </controls:MetroContentControl.Resources>

                <ItemsControl Margin="15" 
                              ItemsSource="{Binding Source={StaticResource cvsActors}}"
                              Style="{StaticResource VirtualizedItemsControlStyle}">
                </ItemsControl>
            </controls:MetroContentControl>
        </Grid>
    </DataTemplate>
</ResourceDictionary>